# Piyare Mobile Telecom SEO

This folder contains dependency-free structured-data helpers for product and local-business SEO.

The production build should inject product JSON-LD into each crawlable product URL and generate a sitemap containing every indexable product URL.
