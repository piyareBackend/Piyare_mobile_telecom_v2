async function initGallery(){const el=document.getElementById('galleryGrid'),wrap=document.getElementById('gallerySection');if(!el||!wrap)return;const fallback=msg=>{wrap.hidden=false;el.innerHTML=`<div class="panel gallery-fallback" role="status">${escapeHtml(msg)}</div>`};try{const response=await pmtGet('content');const data=response?.content?pmtDeepMerge(structuredClone(PMT_DEFAULT_CONTENT),response.content):pmtGetInitialContent();const items=Array.isArray(data.seo?.gallery)?data.seo.gallery.filter(x=>x.image):[];if(!items.length){wrap.hidden=true;return}wrap.hidden=false;el.innerHTML=items.map(x=>`<figure class="gallery-card"><img loading="lazy" decoding="async" src="${escapeHtml(pmtMediaUrl? pmtMediaUrl(x.image):x.image)}" alt="${escapeHtml(x.caption||'Piyare Mobile Telecom')}"><figcaption>${escapeHtml(x.caption||'Piyare Mobile Telecom')}</figcaption></figure>`).join('')}catch(err){console.error('PMT gallery init failed',err);fallback('Gallery is temporarily unavailable. Showing the rest of the site normally.')}}document.addEventListener('DOMContentLoaded',initGallery);window.addEventListener('pmt-content-updated',()=>setTimeout(initGallery,0));
