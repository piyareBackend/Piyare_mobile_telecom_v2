/* PMT product-system hardening. Loaded after app.js and before page-specific product scripts. */
(function(){
  const CACHE_KEY='pmt-public-products-cache-v4',CACHE_TTL=5*60*1000,DRIVE_HOSTS=['drive.google.com','drive.usercontent.google.com','lh3.googleusercontent.com'];
  function imageValue(value){if(value&&typeof value==='object')return value.url||value.src||value.href||value.image||value.fileUrl||value.file||value.id||'';return value||'';}
  function unwrap(value){const s=String(imageValue(value)||'').trim();try{const u=new URL(s,location.origin);if(u.pathname==='/img'){const nested=u.searchParams.get('src');if(nested)return decodeURIComponent(nested)}return s}catch(_){return s}}
  function driveId(value){const s=unwrap(value);if(!s)return '';try{const u=new URL(s,location.origin);if(u.hostname==='drive.google.com'){const m=u.pathname.match(/\/file\/d\/([^/]+)/);if(m)return m[1];const q=u.searchParams.get('id');if(q)return q;}if(u.hostname==='drive.usercontent.google.com'||u.hostname==='lh3.googleusercontent.com')return u.searchParams.get('id')||'';}catch(_){}const m=s.match(/(?:file\/d\/|[?&]id=)([A-Za-z0-9_-]{10,})/);return m?m[1]:'';}
  function directDriveUrl(value){const s=unwrap(value),id=driveId(s);return id?'https://drive.google.com/uc?export=view&id='+encodeURIComponent(id):s;}
  function mediaUrl(value){const s=String(imageValue(value)||'').trim();if(!s)return '';if(s.startsWith('blob:')||s.startsWith('data:'))return s;try{const u=new URL(s,location.origin);if(u.pathname==='/img')return u.toString();if(DRIVE_HOSTS.includes(u.hostname))return location.origin+'/img?src='+encodeURIComponent(directDriveUrl(s));if(driveId(s)&&!/^https?:\/\//i.test(s))return location.origin+'/img?src='+encodeURIComponent(directDriveUrl(s));return u.toString()}catch(_){const id=driveId(s);return id?location.origin+'/img?src='+encodeURIComponent(directDriveUrl(s)):s;}}
  function fallbackCandidates(value){const original=unwrap(value),proxy=mediaUrl(original),direct=directDriveUrl(original),id=driveId(original),out=[];if(proxy)out.push(proxy);if(direct)out.push(direct);if(id)out.push('https://drive.google.com/thumbnail?id='+encodeURIComponent(id)+'&sz=w1200');return [...new Set(out)];}
  window.pmtMediaUrl=mediaUrl;window.pmtImageCandidates=fallbackCandidates;
  window.pmtImageError=function(img,value){let list=[];try{list=img.dataset.pmtCandidates?JSON.parse(img.dataset.pmtCandidates):fallbackCandidates(value)}catch(_){}let i=Number(img.dataset.pmtTry||0)+1;img.dataset.pmtTry=String(i);if(i<list.length){img.src=list[i];return true}return false;};
  document.addEventListener('error',function(e){const img=e.target;if(!(img instanceof HTMLImageElement)||!img.src)return;if(img.dataset.pmtDone==='1')return;img.dataset.pmtDone='1';if(window.pmtImageError&&window.pmtImageError(img,img.src))e.stopImmediatePropagation();},true);
  function normalizeImageList(value){const a=Array.isArray(value)?value:(value?[value]:[]);return a.map(imageValue).map(x=>String(x||'').trim()).filter(Boolean);}
  function normalize(d){const raw=Array.isArray(d)?d:(Array.isArray(d?.items)?d.items:Array.isArray(d?.products)?d.products:Array.isArray(d?.data)?d.data:[]);return raw.filter(Boolean).map((x,i)=>({...x,id:String(x.id??x.ID??x.productId??('product-'+i)),name:String(x.name??x.title??''),category:String(x.category??'Accessories'),price:Number(x.price??0),stock:Number(x.stock??0),description:String(x.description??''),images:normalizeImageList(x.images||x.image||x.imageUrl||x.imageURL),variants:Array.isArray(x.variants)?x.variants:[],mainImage:Math.max(0,Number(x.mainImage)||0),imageSettings:x.imageSettings&&typeof x.imageSettings==='object'?x.imageSettings:{}})).filter(x=>x.name);}
  window.pmtGetPublicProducts=async function(){try{const u=new URL(window.PMT_PUBLIC_API_URL||'/api',location.origin);u.searchParams.set('action','publicProducts');const r=await fetch(u.toString(),{headers:{Accept:'application/json'},cache:'no-store'});const text=await r.text();let d;try{d=JSON.parse(text)}catch(_){throw Error('API returned non-JSON data')}if(!r.ok||d.ok===false)throw Error(d.error||d.message||('API request failed ('+r.status+')'));const items=normalize(d);localStorage.setItem(CACHE_KEY,JSON.stringify({ts:Date.now(),items}));return items}catch(err){try{const c=JSON.parse(localStorage.getItem(CACHE_KEY)||'null');if(c&&Array.isArray(c.items)&&Date.now()-Number(c.ts||0)<=CACHE_TTL)return c.items}catch(_){}throw err}};
  window.PMTProductSEO=function(x,images){const base=location.origin+location.pathname+'?id='+encodeURIComponent(x.id);const set=(selector,attr,value)=>{let el=document.querySelector(selector);if(!el){el=document.createElement('meta');document.head.appendChild(el);if(selector.includes('name='))el.name=selector.match(/name="([^"]+)/)?.[1]||'';else el.setAttribute('property',selector.match(/property="([^"]+)/)?.[1]||'')}el.setAttribute(attr,value||'')};document.title=(x.seoTitle||x.name)+' — Piyare Mobile Telecom';const desc=String(x.seoDescription||x.description||('Buy '+x.name+' from Piyare Mobile Telecom.')).slice(0,300);set('meta[name="description"]','content',desc);set('meta[property="og:title"]','content',x.seoTitle||x.name);set('meta[property="og:description"]','content',desc);set('meta[property="og:type"]','content','product');set('meta[property="og:url"]','content',base);set('meta[property="og:image"]','content',images?.length?mediaUrl(images[Math.max(0,Math.min(Number(x.mainImage)||0,images.length-1))]):'');set('meta[name="twitter:card"]','content','summary_large_image');set('meta[name="twitter:title"]','content',x.seoTitle||x.name);set('meta[name="twitter:description"]','content',desc);let canonical=document.querySelector('link[rel="canonical"]');if(!canonical){canonical=document.createElement('link');canonical.rel='canonical';document.head.appendChild(canonical)}canonical.href=base;const old=document.getElementById('pmt-product-jsonld');if(old)old.remove();const ld=document.createElement('script');ld.id='pmt-product-jsonld';ld.type='application/ld+json';ld.textContent=JSON.stringify({'@context':'https://schema.org','@type':'Product',name:x.name,description:String(x.description||''),image:(images||[]).map(mediaUrl),sku:String(x.sku||x.id),brand:{'@type':'Brand',name:String(x.brand||'Piyare Mobile Telecom')},offers:{'@type':'Offer',price:Number(x.price||0).toFixed(2),priceCurrency:'INR',availability:Number(x.stock||0)>0?'https://schema.org/InStock':'https://schema.org/OutOfStock',url:base}});document.head.appendChild(ld)};
  window.pmtInvalidateProductCaches=function(){try{['pmt-public-products-cache-v2','pmt-public-products-cache-v3','pmt-public-products-cache-v4'].forEach(k=>localStorage.removeItem(k));Object.keys(sessionStorage).filter(k=>k.startsWith('pmt-admin-cache:products:')||k.startsWith('pmt-admin-cache:dashboard:')).forEach(k=>sessionStorage.removeItem(k))}catch(_){} };
  const originalPost=window.pmtPost;if(typeof originalPost==='function'){window.pmtPost=async function(payload){const result=await originalPost.apply(this,arguments);if(/^(createProduct|updateProduct|deleteProduct)$/.test(String(payload&&payload.action||'')))window.pmtInvalidateProductCaches();return result;};}
})();
